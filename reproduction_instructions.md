Instructions to reproduce the results:

Clone this repository

Install the requirements provided in the requirements.txt file with the command pip install -r requirements.txt

Move to the code-folder and run the command python god_network.py to get the graphs with gods and run the command python king_network.py to get the graphs with kings from all periods

After this, the graphs should appear to the figures folder
